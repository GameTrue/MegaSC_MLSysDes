services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    env_file: [.env]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    ports:
      - "8000:8000"
      - "7860:7860"
